spring.datasource.url=jdbc:postgresql://localhost:5432/english_db
spring.datasource.username=muhammed
spring.datasource.password=cocobolo
spring.datasource.driver-class-name=org.postgresql.Driver

# Set to update rather than create to preserve data
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Enable detailed logging for development debugging
logging.level.com.Specific=DEBUG
logging.level.org.springframework.security=DEBUG

# Jackson configuration
spring.jackson.deserialization.fail-on-unknown-properties=false
spring.jackson.serialization.fail-on-empty-beans=false

# Set dev profile by default
spring.profiles.active=dev

# Server port
server.port=8081

# Firebase Configuration
# Set to false for easier development without Firebase
firebase.enabled=false
# Path to your Firebase service account file
firebase.credentials.path=pragmatic-2e3e7-firebase-adminsdk-fbsvc-5d7f9dfa55.json

# DeepL API Configuration
deepl.api.key=83504ff5-0621-4b81-81c5-d050c0f67f9b:fx
deepl.api.url=https://api-free.deepl.com/v2/translate
